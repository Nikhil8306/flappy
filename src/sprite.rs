
pub struct Sprite {

    pub name: String,
    pub tag: String,
    pub level: u32,
    pub sprite: Vec<char>,
    pub height: u32,
    pub width: u32,
    pub render: bool,
    pub x: i32,
    pub y: i32

}